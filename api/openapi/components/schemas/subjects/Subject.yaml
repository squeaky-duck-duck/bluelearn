type: object
additionalProperties: false
properties:
  id:
    $ref: "../common/Uuid.yaml"
  slug:
    type: string
    example: math
  name:
    type: string
    example: Mathematics
required: [id, slug, name]
